CREATE TABLE user (id INT NOT NULL AUTO_INCREMENT, username CHAR(30) NOT NULL, PRIMARY KEY (id));
CREATE TABLE todo_item (id INT NOT NULL AUTO_INCREMENT, deleted TINYINT(1), completed TINYINT(1), content TEXT, user_id INT, PRIMARY KEY (id), FOREIGN KEY (user_id) REFERENCES user(id) ON DELETE CASCADE);